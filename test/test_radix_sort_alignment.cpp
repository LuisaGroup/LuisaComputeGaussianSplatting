/**
 * @file test_radix_sort_alignment.cpp
 * @brief Placeholder - old DeviceParallel alignment tests removed.
 *        LCPP (lc_parallel_primitive) manages temp storage internally.
 * @date 2025-01-09
 */

#include "test_util.h"

TEST_SUITE("radix_sort_alignment") {
    TEST_CASE("placeholder") {
        CHECK(true);
    }
}
